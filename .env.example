# xAI Grok — server-side only, never exposed to the client
XAI_API_KEY=
XAI_MODEL=grok-4.3
XAI_BASE_URL=https://api.x.ai/v1

# --- Crowdsourcing backend (v1) ---
DATABASE_URL=
SESSION_SECRET=
SYBIL_GATE=allow
PASSPORT_API_KEY=
PASSPORT_SCORER_ID=
PASSPORT_MIN_SCORE=15
ADMIN_ADDRESSES=

# AI-maintainer reviewer: "manual"/unset = NoopReviewer (no automated review); "ai" = the agent (when built)
MAINTAINER_REVIEWER=
